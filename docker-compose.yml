version: "3.8"

services:
  genshin-builder:
    image: nginx:alpine
    container_name: genshin-builder
    restart: unless-stopped
    volumes:
      - .:/usr/share/nginx/html:ro
    networks:
      - npm_network

networks:
  npm_network:
    external: true
